<h1>Categories</h1>

<%= link_to "New Category", new_category_path %>

<div id="categories">
    <% @categories.each do |category| %>
        <p><%= link_to category.name, category %></p>
    <% end %>
</div>